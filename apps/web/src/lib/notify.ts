import { toast as sonner } from 'sonner';

/**
 * Всплывашки панели — обычный `toast` из sonner. Раньше каждая писалась в центр уведомлений;
 * теперь колокольчик хранит только то, что требует внимания (серверные события «внимание» и
 * «критично»), а всплывашки от ваших действий просто показываются: всё остальное есть в Журнале.
 */
export const toast = sonner;
