import type { IncidentKind } from '@nodeservice/shared';

/**
 * Плейбуки диагностики (R4.5): порядок проверок и то, как читать результат. Живут в коде, а не в базе
 * знаний: одинаковый метод для всех разборов, правится вместе с инструментами. Каждый плейбук честно
 * называет, чего панель пока не видит, чтобы Джарвис не выдумывал причину.
 */
export interface Playbook {
  id: string;
  title: string;
  when: string;
  /** Виды инцидентов, для которых плейбук подставляется в разбор автоматически. */
  kinds: IncidentKind[];
  steps: string[];
  reading: string[];
  actions: string[];
  limits: string[];
}

export const PLAYBOOKS: Playbook[] = [
  {
    id: 'node_offline',
    title: 'Нода недоступна',
    when: 'Контейнер ноды остановлен, пользователи не подключаются.',
    kinds: ['node_down'],
    steps: [
      'get_incident: что видел зонд, была ли автопочинка и чем кончилась.',
      'get_server_detail: агент в сети, SSH работает, что с CPU, памятью и диском. Если недоступен сам сервер, это плейбук server_unreachable.',
      'check_reachability для порта ноды. Порт ноды панель не знает: спросите администратора или проверьте типичный 443.',
      'inspect_containers: состояние контейнера ноды, код выхода, убит ли по памяти (OOMKilled), число перезапусков, когда остановился (нужно разрешение «Осмотр служб и системы»).',
      'inspect_ports: слушает ли порт ноды какой-нибудь процесс.',
      'inspect_kernel: не убила ли ноду нехватка памяти (OOM-killer).',
      'inspect_node_logs: последние строки журнала ноды за нужный период (нужно разрешение «Логи ноды»).',
      'get_maintenance: не было ли обновлений или перезагрузки незадолго до сбоя.',
    ],
    reading: [
      'Агент в сети, SSH работает, контейнер остановлен: остановился только контейнер. Первый шаг «Поднять контейнер ноды» (T1).',
      'Контейнер поднят, а порт закрыт со всех проверяющих: сервис внутри не слушает порт или порт закрыт фильтром у хостера.',
      'Порт открыт с одних проверяющих и закрыт с других: блокировка или сбой маршрута для части направлений.',
      'Контейнер падает снова после подъёма: причина внутри (память, конфигурация, обновление образа). Код выхода и OOMKilled даёт inspect_containers, подробности inspect_node_logs.',
      'OOMKilled=true или в журнале ядра «Out of memory: Killed process»: не хватает памяти, перезапуск не лечит причину.',
    ],
    actions: [
      'node_up (T1) первым.',
      'Перезагрузка сервера (T3) только вручную, если контейнер не поднимается и есть признаки зависшей системы.',
      'Если осмотр или логи выключены в разрешениях, назовите это и предложите команду текстом: docker logs --tail 100 remnanode (T3).',
    ],
    limits: [
      'Без разрешений на осмотр и логи ноды причина падения не видна: скажите об этом. Конфигурацию Xray внутри контейнера и то, что видит пользователь, панель не читает.',
    ],
  },
  {
    id: 'server_unreachable',
    title: 'Сервер или агент не отвечает',
    when: 'Нет SSH или агент не в сети.',
    kinds: ['ssh_down', 'agent_offline'],
    steps: [
      'get_incident и get_server_detail: что именно молчит, SSH или только агент, когда это началось.',
      'check_reachability для порта SSH этого сервера.',
    ],
    reading: [
      'SSH закрыт со всех проверяющих и агента нет: сервер выключен, завис или отрезан у хостера. Проверять надо в панели хостера, панель сама ничего не сделает.',
      'SSH открыт с проверяющих, а панель не подключается: возможна блокировка адреса панели, смена ключа или пароля.',
      'SSH работает, а агент молчит: остановился сервис агента. Шаг «Переустановить агента» (T2).',
      'Порт SSH открыт не со всех проверяющих: блокировка или сбой маршрута до части сети.',
    ],
    actions: [
      'agent_reinstall (T2) только если SSH работает.',
      'Если SSH работает, причину молчания агента даёт inspect_logs с целью agent (нужно разрешение «Журналы служб»); иначе предложите команду текстом: systemctl status nodeservice-agent и journalctl -u nodeservice-agent -n 50 (T3).',
      'Если SSH недоступен, действий у панели нет: скажите об этом прямо.',
    ],
    limits: ['Состояние сервера у хостера панель не видит.'],
  },
  {
    id: 'disk_full',
    title: 'Диск заполнен',
    when: 'Диск выше порога.',
    kinds: ['disk_high'],
    steps: [
      'get_incident: попытки чистки, что освободили, вывод осмотра диска (крупные каталоги, временные файлы).',
      'get_metrics_history для diskPct за 24 часа: ровный рост означает, что кто-то пишет постоянно, скачок означает разовый файл.',
      'get_maintenance: свободное место и предупреждения.',
      'inspect_disk: занятость файловых систем, самые тяжёлые каталоги, место под Docker и размер журнала systemd (нужно разрешение «Осмотр служб и системы»).',
    ],
    reading: [
      'Осмотр показал много временных файлов старше часа: «Очистить временные файлы» (T2).',
      'Чистка журнала, образов и кэша вернула меньше 1 ГБ: причина не в них.',
      'Диск растёт ровно даже после чистки: источник продолжает писать, чистка поможет ненадолго. Ищите источник.',
      'Крупные журналы контейнера в /var/lib/docker/containers: нужна ротация логов Docker.',
    ],
    actions: [
      'free_disk и apt_clean (T1), disk_inspect (T0), tmp_clean (T2).',
      'Командой текстом (T3): docker system df и du -xh /var/lib/docker/containers --max-depth=1 | sort -h | tail.',
      'Никогда не предлагайте docker system prune: он удаляет и остановленные контейнеры, в том числе остановленную ноду.',
    ],
    limits: ['Какой процесс пишет на диск, панель не видит, только куда он пишет (по осмотру).'],
  },
  {
    id: 'high_load',
    title: 'Высокая нагрузка на CPU или память',
    when: 'CPU или память выше порога.',
    kinds: ['cpu_high', 'mem_high'],
    steps: [
      'get_incident и get_metrics_history для cpuPct или memPct за час и за сутки: пик, плавный рост или ровная полка.',
      'inspect_processes: кто грузит процессор и память.',
      'get_metrics_history для netRxBps и netTxBps: нагрузка совпадает с трафиком или нет.',
      'inspect_kernel: не убивал ли процессы OOM-killer (для памяти); inspect_containers: перезапуски и OOMKilled.',
    ],
    reading: [
      'Главный процесс xray или контейнер ноды и трафик высокий: нагрузка от пользователей. Перезапуск снимет пик, но не причину.',
      'Главный процесс не нода (обновление apt, резервная копия, чужой сервис): перезапуск ноды не поможет.',
      'Память растёт без спадов: похоже на утечку, перезапуск ноды даёт временный эффект.',
      'Load высокий, а CPU низкий: ждут диск или сеть, посмотрите диск.',
    ],
    actions: [
      'restart_node (T2) только если нагрузку создаёт нода. Предупредите, что соединения оборвутся на несколько секунд.',
      'Если нагрузку создаёт другой процесс, предложите разобраться с ним вручную (T3, текстом).',
    ],
    limits: [
      'Командные строки процессов панель не читает (только имена). Журнал ноды доступен через inspect_node_logs, если разрешён.',
    ],
  },
  {
    id: 'conntrack_full',
    title: 'Забит conntrack',
    when: 'Часть пользователей не подключается при нормальных CPU и памяти, число соединений на пределе.',
    kinds: [],
    steps: [
      'get_metrics_history для conntrackCount за сутки: рост вместе с трафиком или скачок от сканеров и коротких соединений.',
      'get_server_detail: память сервера, от неё зависит безопасный лимит.',
      'inspect_processes: не воюет ли что-то с соединениями.',
      'inspect_kernel: есть ли в журнале ядра «nf_conntrack: table full, dropping packet» и когда (нужно разрешение «Осмотр служб и системы»).',
    ],
    reading: [
      'Число соединений упёрлось в потолок и держится на нём: новые соединения отбрасываются (в dmesg «nf_conntrack: table full, dropping packet»).',
      'Потолок панель не знает, она видит только текущее число: сравнить с nf_conntrack_max нужно на сервере.',
      'Причины: много коротких соединений (сканеры, торренты, атака) или слишком малый лимит для объёма трафика.',
    ],
    actions: [
      'Командами текстом (T3): cat /proc/sys/net/netfilter/nf_conntrack_count /proc/sys/net/netfilter/nf_conntrack_max.',
      'Поднять лимит вручную (T3, текстом): sysctl -w net.netfilter.nf_conntrack_max=1048576 и записать в /etc/sysctl.d/. Каждая запись занимает память, учтите объём RAM.',
    ],
    limits: [
      'Сообщения ядра видны через inspect_kernel, если разрешён осмотр. Сам потолок nf_conntrack_max панель не читает: команда текстом.',
    ],
  },
  {
    id: 'tspu_degradation',
    title: 'Деградация из-за ТСПУ',
    when: 'Скорость падает и соединения рвутся у части пользователей или у отдельных провайдеров, при этом сервер здоров.',
    kinds: [],
    steps: [
      'get_server_detail и get_metrics_history для CPU, памяти, netRxBps и netTxBps: сервер в норме, а трафик просел?',
      'check_reachability для порта ноды: открыт ли он со всех проверяющих.',
      'get_fleet_status: то же у других нод парка или только у этой.',
    ],
    reading: [
      'Сервер здоров, порт открыт со всех проверяющих, а пользователи жалуются: похоже на замедление или блокировку на стороне их провайдера, а не сбой сервера.',
      'Порт открыт не со всех проверяющих: блокировка или сбой маршрута на пути.',
      'Проседание у всех российских нод в одно и то же время: замедление у операторов, а не проблема конкретной ноды.',
    ],
    actions: [
      'Панель ничего не чинит. Смена порта, транспорта или SNI меняет конфигурацию ноды: только вручную и текстом (T3), автоматически не предлагайте.',
    ],
    limits: [
      'Ретрансмиты, RST, скорость канала и признаки ТСПУ панель пока не собирает (этап проверок). Скажите прямо, что вывод по косвенным признакам.',
      'Проверка идёт с серверов парка, а не из сети пользователей.',
    ],
  },
  {
    id: 'domain_blocked',
    title: 'Блокировка домена',
    when: 'Домен ноды или маскировки перестал открываться.',
    kinds: [],
    steps: [
      'get_server_detail: адрес сервера, домен это или IP.',
      'check_reachability для порта 443 (и порта ноды): смотрите поле dns и доступность порта у проверяющих.',
    ],
    reading: [
      'DNS отвечает по-разному у проверяющих или не тем адресом, что у сервера: подмена DNS или блокировка по домену.',
      'DNS одинаковый и верный, порт открыт со всех: домен на этих путях не блокируется, причину ищите у пользователей.',
      'Порт закрыт только с части проверяющих: блокировка по адресу или маршруту, а не по домену.',
    ],
    actions: ['Панель ничего не чинит. Смена домена или записей DNS делается вручную и текстом (T3).'],
    limits: ['Реестр блокировок и проверка из России пока недоступны (этап проверок).'],
  },
  {
    id: 'gemini_ru',
    title: 'Сервис видит российский IP выхода',
    when: 'Gemini или другой сервис с региональными ограничениями не работает через ноду.',
    kinds: [],
    steps: [
      'get_server_detail: адрес и провайдер выходной ноды.',
      'Гео-разметки IP у панели пока нет: попросите администратора проверить страну выхода командой текстом.',
    ],
    reading: [
      'Выход ноды в России: сервис видит российский адрес и может ограничить доступ. Нужна выходная нода за границей.',
      'Выход за границей, а сервис всё равно не работает: причина в другом (подсеть хостера в чёрном списке сервиса, DNS, маршрутизация).',
    ],
    actions: ['Команда текстом (T3): curl -s https://ipinfo.io/country с сервера ноды.'],
    limits: ['Страну IP и репутацию подсети панель не определяет (этап проверок).'],
  },
];

export const playbookById = (id: string): Playbook | undefined => PLAYBOOKS.find((p) => p.id === id);

export const playbookForKind = (kind: IncidentKind): Playbook | undefined =>
  PLAYBOOKS.find((p) => p.kinds.includes(kind));

/** Плейбук текстом для модели. */
export function renderPlaybook(p: Playbook): string {
  const list = (items: string[]) => items.map((x, i) => `${i + 1}. ${x}`).join('\n');
  return [
    `ПЛЕЙБУК «${p.title}» (${p.id}). Когда: ${p.when}`,
    `Что проверить по порядку:\n${list(p.steps)}`,
    `Как читать результат:\n${list(p.reading)}`,
    `Что предлагать (уровни T0–T3 из реестра, T3 только текстом):\n${list(p.actions)}`,
    `Чего панель не видит (скажите об этом прямо):\n${list(p.limits)}`,
  ].join('\n\n');
}
